{% extends 'base.html.twig' %}

{% block title %}Member{% endblock %}

{% block body %}
    <h1>Member</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ member.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ member.name }}</td>
            </tr>
            <tr>
                <th>Role</th>
                <td>{{ member.role }}</td>
            </tr>
            <tr>
                <th>Age</th>
                <td>{{ member.age }}</td>
            </tr>
            <tr>
                <th>Team</th>
                <td>{{ member.teamid }}</td>
            </tr>
        </tbody>
    </table>

    <a class="btn btn-outline-dark" href="{{ path('app_member_index') }}">back to list</a>

    <a class="btn btn-outline-dark" href="{{ path('app_member_edit', {'id': member.id}) }}">edit</a>

    {{ include('member/_delete_form.html.twig') }}
{% endblock %}
